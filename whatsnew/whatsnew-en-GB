• Fixed searching the Item Price list: typing a single word such as an item name now finds matching prices instead of returning nothing. Any list whose records have no dedicated search fields is searched by the name shown on the row, so what you see is what you can search for.
