• Fixed: POS Upload search now finds POS Uploads (it previously looked up POS Invoices), so tapping a result opens the right document.
• Fixed: a tiny layout glitch in the document header while scrolling forms.
