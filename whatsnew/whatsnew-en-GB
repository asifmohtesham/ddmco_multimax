• Session Defaults: set a Default Warehouse (Menu → Account & settings → Session Defaults).
• Dashboard search: each matching item now shows its stock balance in your Default Warehouse, right on the result row — summed across child warehouses when the default is a group warehouse.
