• Default Warehouse now lets you pick a warehouse group (shown with a "Group" tag), not just a single warehouse.
• Dashboard search shows each matching item's stock in your Default Warehouse right on the result row — summed across all child warehouses when you pick a group.
